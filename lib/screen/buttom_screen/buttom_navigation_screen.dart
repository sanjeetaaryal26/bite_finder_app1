import 'package:flutter/material.dart';

class ButtomNavigationScreen extends StatelessWidget {
  const ButtomNavigationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox.expand(child: Text('Button Navigation Screen'),);
  }
}